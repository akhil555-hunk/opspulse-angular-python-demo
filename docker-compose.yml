services:
  backend:
    build: ./backend
    container_name: opspulse-backend
    expose:
      - "5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: opspulse-frontend
    ports:
      - "8082:80"
    depends_on:
      - backend
    restart: unless-stopped
